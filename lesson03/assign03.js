let arr = ['Honda', 'Audi', 'Toyota', 'Subaru', 'Chevy'];

for (let i = 0; i < arr.length; i++) {
  console.log(i + ', ' + arr[i]);
}

//Start at 0 index, Run loop until i is less than array length, increment

//Print index, print , with space, print string in array
