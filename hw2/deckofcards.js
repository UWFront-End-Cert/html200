const suits = ['hearts', 'clubs','diamonds', 'spades']
const values =['ace', '2','3','4','5','6','7','8','9','10', 'jack', 'queen', 'king']

for ( let s = 1; s <5; s++) {
  for ( let v= 1; v <14 ; v++){
    console.log( s,v)
  }
}
