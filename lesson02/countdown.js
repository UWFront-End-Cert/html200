// use one loop to console log a variable number
// decrease it by one and log that number
// stop after the number prints 0

let countdown = 2;

while (countdown > 0) {
  countdown--;
  console.log(countdown);
}
